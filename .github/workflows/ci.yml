name: CI Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test:
    name: Type Check & Unit Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'
        cache-dependency-path: './typeflow/package-lock.json' # Since it's inside ai-factory/typeflow

    - name: Install dependencies
      working-directory: ./typeflow
      run: npm ci

    - name: Run Type Check and Vitest
      working-directory: ./typeflow
      run: npm run test
